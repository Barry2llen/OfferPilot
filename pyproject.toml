[project]
name = "offerpilot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "langchain>=1.2.15",
    "langgraph>=1.1.6",
    "pydantic>=2.13.0",
]
